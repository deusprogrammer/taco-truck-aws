{
  "name": "tt-store",
  "version": "1.0.0",
  "description": "",
  "main": "tt-store.js",
  "scripts": {
    "package": "zip -r ../tt-lambdas.zip .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.17.1",
    "aws-sdk": "^2.1692.0"
  }
}
